export class Company {
    constructor(public id: string, public name: string, public price: number, public employees: number, public sector: string, public industry: string, public region_id: string,) { }
}
